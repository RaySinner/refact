# Media placeholders

This directory holds preserved logo assets and placeholder GIFs for the README rewrite.

The GIFs in this directory are **PLACEHOLDERS** generated by `tools/dev/make_placeholder_gifs.py`. Replace them with real product recordings before release. Final recordings should be about 900px wide, no larger than 8MB each, and loop cleanly.

| File | What to record |
| --- | --- |
| `hero.gif` | IDE: ask the agent → it plans, edits, runs checks. |
| `agent-task-planner.gif` | Task Planner board + cards + plan. |
| `chat-modes.gif` | Mode switch (Ask→Agent→Plan) + transitions. |
| `code-completion.gif` | Inline FIM completion. |
| `auto-apply.gif` | Agent edits + auto-apply patch + checkpoint. |
| `browser-tool.gif` | Browser automation: navigate and screenshot. |
| `memory.gif` | Knowledge save + autoinjection as context. |
| `buddy.gif` | Buddy surfacing opportunities, launching investigations, and offering guarded actions. |
| `mcp-skills.gif` | MCP, skills, slash commands, subagents, and marketplace install. |
| `any-device.gif` | UI streamed from the Rust core, opened in a browser or another device. |

Preserved logo copies:

- `logo-light.svg` copied byte-for-byte from `docs/src/assets/logo-light.svg`.
- `logo-dark.svg` copied byte-for-byte from `docs/src/assets/logo-dark.svg`.
